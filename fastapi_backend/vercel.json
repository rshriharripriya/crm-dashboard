{
"buildCommand": "conda activate crm-dashboard && pip install -r requirements.txt && alembic upgrade head",
  "outputDirectory": "api",
  "git": {
    "deploymentEnabled": {
      "main": false
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}